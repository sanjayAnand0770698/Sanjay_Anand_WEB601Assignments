import { SeriesTypePipe } from './series-type.pipe';

describe('SeriesTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SeriesTypePipe();
    expect(pipe).toBeTruthy();
  });
});
