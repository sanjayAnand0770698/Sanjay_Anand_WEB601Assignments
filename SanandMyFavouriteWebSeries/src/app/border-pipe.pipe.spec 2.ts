import { BorderPipePipe } from './border-pipe.pipe';

describe('BorderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BorderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
